services:
  sync-tool:
    build: .
    container_name: eshop-sync-tool
    restart: unless-stopped
    ports:
      - "2050:2050"
    volumes:
      - ./config.json:/app/config.json
    environment:
      - NODE_ENV=production
      - TZ=Asia/Shanghai
