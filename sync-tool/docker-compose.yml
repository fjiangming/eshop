services:
  sync-tool:
    build: .
    container_name: eshop-sync-tool
    restart: unless-stopped
    ports:
      - "2050:2050"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - TZ=Asia/Shanghai
